media:
  input: content
  output: /stargate-wiki
  categories: [image]
content:
  - name: wiki
    label: Wiki
    type: collection
    path: content
    filename: "{primary}.md"
    view:
      fields: [ title, slug ]
    fields:
      - { name: title, label: Title, type: string, required: true }
      #- { name: slug, label: Slug, type: string, required: true }
      - { name: type, label: Type, type: select, required: true, options: { values: [docs] } }
      - name: body
        label: Body
        type: rich-text